# MATLAB Simulation Report — Digital Twin for WRO Future Engineers

## Table of Contents
- [Problem Statement](#problem-statement)
- [Evolution: From Visualizer to Digital Twin](#evolution-from-visualizer-to-digital-twin)
- [Part A — Open Challenge Visualizer (Season 1)](#part-a--open-challenge-visualizer-season-1)
- [Part B — Obstacle Challenge Digital Twin (Season 2)](#part-b--obstacle-challenge-digital-twin-season-2)
  - [Objectives](#objectives)
  - [Simulation Architecture](#simulation-architecture)
  - [Perception: Running Our Real Vision Code](#perception-running-our-real-vision-code)
  - [Navigation and State Machine](#navigation-and-state-machine)
  - [Official WRO Randomization](#official-wro-randomization)
  - [Full Mission: Parking and Both Directions](#full-mission-parking-and-both-directions)
  - [Engineering Iterations](#engineering-iterations)
  - [Validation Results](#validation-results)
- [Power Budget: Prediction → Measurement → Verification](#power-budget-prediction--measurement--verification)
- [Closing the Loop: Simulation-Tuned Parameters](#closing-the-loop-simulation-tuned-parameters)
- [How to Run](#how-to-run)
- [Known Limitations](#known-limitations)
- [Conclusion](#conclusion)

---

## Problem Statement

Testing robot navigation on the physical WRO arena is slow, unrepeatable, and risky.
The Obstacle Challenge randomizes the traffic-sign layout **before every round**
(coin tosses + 36 official cards), so a strategy that works on one layout can fail
on the next — and physically rebuilding dozens of layouts to test one code change
is impractical. Every crash also risks the steering servo and camera mount.

Our answer was to build a MATLAB **digital twin**: a simulated arena where our
robot's actual perception strategy runs in closed loop, every test layout is
generated by the official randomization procedure, and performance is measured
in numbers instead of impressions.

## Evolution: From Visualizer to Digital Twin

| Season 1 (Open Challenge) | Season 2 (Obstacle Challenge) |
|---|---|
| Robot followed **pre-placed waypoints** | Robot is **sensor-driven**: acts only on what the simulated camera and distance rays perceive |
| No obstacles | Red/green signs placed by the **official coin-and-cards procedure** |
| Visualization only | **Closed loop**: perception → decision → actuation → physics → perception |
| Judged by watching | Judged by **metrics**: pass rate, clearances, lap times, parking verdicts |

The key upgrade: our first simulation could not fail. The new one fails exactly
where our algorithm is weak — which is the entire point of simulating.

## Part A — Open Challenge Visualizer (Season 1)

Our first MATLAB module modeled the arena in 3D with the official mat texture,
a cube robot with car-like steering (limited turn rate, 0.12 m/s), Gaussian
"wiggle" noise to imitate motor vibration, and a chase camera. It followed four
waypoints anticlockwise around the central square.

**Figure 1** – Front chase-camera view (Season 1).

![Front Camera View](./matlab_report/images/page_4_image_1.png)

![WRO Mat Texture](./matlab_report/images/page_5_image_1.png)

Code: [`matlab/open_challenge.m`](./matlab/open_challenge.m)

What it taught us: visual debugging and steering physics — and its limits.
Because the path was pre-programmed, it could never tell us whether our
*perception and decision logic* would survive a randomized field. That gap
defined the requirements for Season 2.

## Part B — Obstacle Challenge Digital Twin (Season 2)

### Objectives

1. Reproduce the field to official dimensions (3×3 m mat, 1×1 m centre box,
   sign seats at the exact card positions: rows 400/600 mm from the outer wall).
2. Run our robot's **real vision strategy** (HSV block detection + proportional
   centroid steering from `src/obstacle_challenge/`) against a simulated camera.
3. Generate every test layout with the **official randomization procedure**.
4. Complete the full mission: **3 laps + parallel parking**, in both driving
   directions, without touching walls, signs, or parking barriers.
5. Measure everything and validate statistically over batches of layouts.

### Simulation Architecture

One shared control step is used by the interactive simulator and the headless
batch evaluator, so the two can never drift apart.

| Module | Role | Real-robot counterpart |
|---|---|---|
| `run.m` | Interactive sim: live map, simulated camera, telemetry, Randomize button | debug UI |
| `control_step.m` | One 30 Hz perception + decision + physics step | main control loop |
| `render_frame.m` | Simulated 640×360 camera: walls, pillars, magenta barriers | Picamera2 |
| `find_biggest_block.m` | ROI crop → HSV masks → largest portrait blob | `camera.py` |
| `ray_wall_distance.m` | Ray-cast wall distances | VL53 ToF sensors |
| `place_obstacles_random.m` | Official coin + 36-card randomization | judges' procedure |
| `parking_control.m` | Post-lap-3 parking maneuver | parking routine |
| `simulate_run.m` / `evaluate.m` | Headless runs / batch statistics | test harness |
| `sweep_kp.m` | Steering-gain sweep on paired layouts | tunes `config.py` |
| `power_budget.m` | Electrical model: rails, battery, runtime | battery/regulator selection |

### Perception: Running Our Real Vision Code

The simulated pipeline ports the robot's code line by line: the same 640×360
frame size, the same ROI crop, the same HSV thresholds (red split across the
hue wrap, 0–10 and 170–180), the same portrait filter (`height > width`) that
rejects reflections, and the same steering law:

```
servo = clamp( KP_BLOCK × (cx − target_x), ±45° )
red   → target_x = 20   (pass on its right)
green → target_x = 620  (pass on its left)
```

An HSV threshold that fails in simulation fails on the robot for the same reason.

### Navigation and State Machine

We tested two architectures and kept the one that survived:

- **Layered overrides (rejected):** track steering + wall repulsion + block
  avoidance added as separate corrections. Near a wall the corrections
  cancelled and the robot deadlocked — observed repeatedly.
- **Unified vector field (final):** every influence is a direction vector;
  steering follows the summed direction. The lane-flow component always points
  along the track, so the result is never zero: the robot always has
  somewhere to go.

Block avoidance pulls toward a **pass point** 0.30 m beside each pillar,
anchored to the lane direction at the pillar (world frame). Our first version
anchored it to the robot's heading — the push direction rotated with the robot
and it orbited pillars in circles. World-frame anchoring made orbiting
geometrically impossible.

States: `DRIVING_STRAIGHT → AVOIDING_BLOCK → CLEARING → (back)`, with
`EMERGENCY_REVERSE` as a guarded last resort and a stuck watchdog (< 6 cm of
motion in 3 s forces a back-off) as the safety net of last resort.

### Official WRO Randomization

`place_obstacles_random.m` implements the rulebook procedure, cross-checked
against the official fe-randomization-app source: one coin-chosen section gets
a single sign at X2; the other three sections draw cards (never two signs on
one radius, X seats only as singles); at least one mixed-colour same-row pair
is guaranteed; seats in front of the start zone are excluded; the parking
section allows only inner-row seats. Every simulated round is a legal
competition layout — including the worst ones.

### Full Mission: Parking and Both Directions

- Parking lot to rule: two magenta barriers (200×20×100 mm) on the outer wall
  of the start section, gap = 1.5 × robot length. Barriers are solid physics
  objects, rendered by the camera, and **every touch is counted** (rule
  9.24.7 ends a real round on contact).
- Maneuver after lap 3: approach → align → turn in → enter → straighten.
  Verdict mirrors scoring: *full* (fully inside, parallel, zero touches)
  vs *partial*.
- Driving direction (CW/CCW) randomizes every round; the start zone mirrors
  and the lot moves to the opposite half, as on the real field.

### Engineering Iterations

Every design change was driven by an observed failure:

| # | Observed failure | Root cause | Fix |
|---|---|---|---|
| 1 | Endless left/right oscillation | Avoidance target ±180° from heading flipped the error sign every frame | Heading-relative targets; later replaced by vector field |
| 2 | Robot pinned against wall | Steering corrections cancelled to zero | Re-architecture: unified vector field |
| 3 | ~10 reverse cycles per sign | Reverse trigger too eager + avoidance turned back into the block's lane | Narrower trigger + cooldown + decaying push in CLEARING |
| 4 | Loop-the-loop at pillars | Heading-relative push rotates with the robot (orbit feedback) | World-frame pass points |
| 5 | Wedged between sign and outer wall | Wall repulsion covered the whole legal corridor | Repulsion range cut to 0.22 m |
| 6 | Run wasted at spawn | Randomizer ignored start-zone seat rules | Official forbidden-seat rule implemented |
| 7 | Simulation lag | Full-frame temporaries, 30 Hz HSV, console spam | Cached buffers, 10 Hz half-res vision — 23× real time |
| 8 | 55 barrier touches while parking | Diagonal reverse entry grazed the barriers | Align-then-enter maneuver: zero touches by construction |

### Validation Results

**12 officially randomized layouts, random directions, reproducible (seed 42):**

```
RESULT: 12 / 12 missions complete (3 laps + parked)   (100%)
Full parallel parks (15 pts): 12 / 12    Barrier touches: 0 total
Avg mission time: 148.2 s    (5 CW rounds, 7 CCW rounds)
Worst clearance to any sign: 0.14 m — no sign moved, ever
```

![Batch evaluation](./matlab/obstacle_challenge/results/evaluation_12runs_2026-07-12_181137.png)

Per-run reports (trajectory coloured by state, speed profile,
clearance-vs-time, summary stats) are saved automatically after every run.

## Power Budget: Prediction → Measurement → Verification

1. **Prediction.** With datasheet estimates, `power_budget.m` flagged a
   brownout risk: a Pi 5 and a stalling steering servo could overload a
   generic 3 A regulator exactly when the robot fights an obstacle.
2. **Measurement.** We measured the actual vehicle: our regulator is an
   XY-3606 rated 5 A, the Pi draws 5.7 W under full vision (PMIC, no
   throttling), the servo stalls at ~0.8 A, and whole-robot battery current
   is 0.40 A idle / 0.80 A driving.
3. **Verification.** Peak 5 V demand is 2.3 A against 5 A — brownout ruled
   out with 2.2× headroom, and ~102 min runtime on our 1500 mAh 3S pack.
   The model now matches the multimeter within 10% at cruise, which is what
   lets us trust its answer for the one case we can't safely measure:
   worst-case stall (44 W, 2.6 C — inside every rating).

![Power budget](./matlab/obstacle_challenge/results/power_budget.png)

## Closing the Loop: Simulation-Tuned Parameters

`sweep_kp.m` swept our camera steering gain across the **same six randomized
layouts per value** (paired seeds):

| KP_BLOCK | Pass rate | Avg mission time |
|---|---|---|
| 0.05 | 6/6 | 131.4 s |
| 0.07 | 6/6 | **129.7 s** |
| **0.09 (deployed)** | 6/6 | 132.6 s |
| 0.12 | 6/6 | 140.3 s |
| 0.15 | 6/6 | 159.5 s |

Our deployed 0.09 sits in the flat optimum; gains ≥ 0.12 cost up to +20%
mission time through oversteer. The physical tuning session can skip the
high-gain region entirely.

![KP sweep](./matlab/obstacle_challenge/results/kp_sweep.png)

## How to Run

MATLAB R2024a+ with Image Processing Toolbox.

```matlab
cd matlab/obstacle_challenge
run              % interactive simulator (Randomize button between runs)
evaluate(12, 42) % batch: 12 official layouts, reproducible
sweep_kp         % steering-gain sweep (~2 min)
power_budget     % electrical checks
test_headless    % 5-second smoke test
```

## Known Limitations

Stated openly, because knowing a model's limits is part of using it:

- **Circular lane approximation** in the default navigation mode; an
  experimental port of the robot's real gyro/corner logic exists
  (`P.nav_mode='gyro'`) but is not yet tournament-grade.
- **Parking entry uses a pivot turn-in** — our unicycle model rotates in
  place; the physical Ackermann car needs a reversing S-curve. Geometry and
  scoring transfer; the entry trajectory does not.
- **Idealized camera** — no noise, blur, or lighting variation, so simulated
  clearances are optimistic bounds.
- **No orange/blue line detection** — the sim counts laps geometrically.

## Conclusion

Across two seasons this module evolved from a visualization into an
engineering instrument. It runs our real perception strategy in closed loop
on officially randomized layouts, and it converts "it seems to work" into
**12/12 missions with 12/12 full parallel parks and zero contact**. It caught
control bugs we would never have staged physically, verified our power system
with measured data, and bounded our steering gain before a single physical
tuning session. Every result above is reproducible from this repository with
one command and one seed.
